# remarks/urls.py
from django.urls import path
from . import views

app_name = 'remarks'  # Add namespace

urlpatterns = [
    # path('counselor_insights/', views.counselor_insights, name='counselor_insights'),
    # path('student_remarks/', views.student_remarks, name='student_remarks'),
]
